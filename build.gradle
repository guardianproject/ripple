buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:1.5.0'
        classpath files('libs/gradle-witness.jar')
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
