buildscript {
    repositories {
        jcenter()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:2.2.2'
        classpath files('libs/gradle-witness.jar')
    }
}

allprojects {
    repositories {
        jcenter()
    }
}
